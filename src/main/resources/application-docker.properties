# Configurações específicas para Docker
server.port=8080

# No futuro, aqui você configuraria a URL do banco PostgreSQL do Docker
# spring.datasource.url=jdbc:postgresql://db:5432/deliverydb

# Mantém o H2 por enquanto
spring.datasource.url=jdbc:h2:mem:deliverydb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Swagger continua ativo
springdoc.swagger-ui.enabled=true
